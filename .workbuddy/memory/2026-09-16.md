

## 2026-09-16 桌宠动画重制（进行中）
- 诊断：旧跑帧 AI 逐张生成导致颜色漂移（橙→黄）与姿势跳变；走路仅4帧太僵。
- 方案定稿：每形象 walk 8帧 + run 6帧（卡通步态标准），朝右基准生成，_l 程序镜像。
- 小猫样板完成：sprites/anim_v2/ 共28个PNG（640x600 RGBA，含_l），预览GIF在 .workbuddy/anim_work/preview_walk.gif、preview_run.gif。
- 生产管线：miora 以各形象 walk1.png 洋红底合成图为锚点参考逐帧生成 -> python 后期（边缘泛洪去洋红底+内部噪点修补+去色染+高度归一 480/430 宽度夹632+底部对齐y=578居中x=320）。
- 处理脚本：C:/Users/yhw/.workbuddy/binaries/python/workspace/process_cat_frames.py（其余4形象复用时改 FRAMES 字典与路径即可）。
- 待办：用户确认小猫样板后批量生产 capybara/husky/panda/redpanda（各14帧）；之后可能需改 core.py RUN_FRAMES/WALK_FRAMES 接入新素材。


## 2026-09-16 增帧返工
- 用户反馈8/6帧不连贯 -> 升级为走16帧+跑12帧：现有帧作关键帧，AI 以锚点+前后关键帧三图参考补生成中间帧（14张）。
- 修复：cutout 后先按 alpha 包围盒紧裁再缩放，否则角色在源图偏置时会被 640x600 画布裁角（walk12 变小问题）。
- 归一化结果：走路全帧 480 高、跑动全帧 430 高，零方差；GIF 预览 walk 85ms/帧、run 50ms/帧。
- 小猫成品 56 个文件（walk1..16/run1..12 + _l 镜像），画布已更新28项。
